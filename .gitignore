# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lambda zip artifact (regenerated in CI)
lambda.zip

# Local docs - not committed (used to regenerate bonds-context.md locally)
docs

# Local lambda context regeneration output (committed version lives in repo)
# lambda/bonds-context.md IS committed
# Environment files (contain secrets)
.env
.env.*
.env.local
.env.production
